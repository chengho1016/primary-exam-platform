{
  "name": "primary-exam-platform",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm install && npx prisma generate && npx prisma db push && npm run build"
  },
  "dependencies": {
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
