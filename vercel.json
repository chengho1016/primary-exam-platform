{
  "buildCommand": "cd apps/web && npx prisma generate && npx prisma db push && npm run build",
  "installCommand": "npm install && cd apps/web && npm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
