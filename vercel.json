{
  "buildCommand": "cd apps/web && npm install && npx prisma generate && npx prisma db push && npm run build",
  "installCommand": "cd apps/web && npm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}
